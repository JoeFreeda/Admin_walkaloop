<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin_walkaloop">

    <application
        android:allowBackup="true"
        android:icon="@drawable/walk_the_loop"
        android:label="@string/app_name"
        android:roundIcon="@drawable/walk_the_loop"
        android:supportsRtl="true"
        android:theme="@style/Theme.Admin_walkaloop">
        <activity android:name=".ui.login_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity_map"></activity>
        <activity android:name=".ui.MainActivity"></activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>